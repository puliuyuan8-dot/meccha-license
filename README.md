# Meccha License Revocation List

This repository hosts the public revocation list used by the internal Meccha Camouflage team build.

Only device codes that should be blocked belong in `revocation-list.json`.
